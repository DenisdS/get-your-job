import React, { PureComponent, ReactElement } from 'react'

export default class About extends PureComponent {

  render = (): ReactElement => {

    return (
      <section>
      </section>
    )
  }
}
