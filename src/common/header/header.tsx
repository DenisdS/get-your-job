import React, { ReactElement } from 'react'

type Props = {

}

const HeaderComponent = ({

}: Props): ReactElement => {

  return (
    <header>
      Olá!
    </header>
  )
}

export default HeaderComponent
