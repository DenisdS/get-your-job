import React, { PureComponent, ReactElement } from 'react'

export default class Home extends PureComponent {

  render = (): ReactElement => {

    return (
      <section>
      </section>
    )
  }
}
