import React, { PureComponent, ReactElement } from 'react'

export default class Jobs extends PureComponent {

  render = (): ReactElement => {

    return (
      <section>
      </section>
    )
  }
}
